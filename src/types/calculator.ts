export type Product = {
  id: string;
  name: string;
};

export type FullLineInputs = {
  weight: string;
  density: string;
  thickness: string;
  area: string;
};

export type AgglomerateInputs = {
  density: string;
  area: string;
  kg: string;
};